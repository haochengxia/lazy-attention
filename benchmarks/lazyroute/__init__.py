"""LazyRoute: query-adaptive sparse decode over reusable KV blocks.

Measurement and engine-side harnesses for the plan in PROJECT.md. Nothing here
is imported by `lazy` itself -- these are instruments, not engine code.
"""
